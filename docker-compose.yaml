version: "3.0"
services:
  main:
    build: .
    command: python3 main.py
    network_mode: host
    working_dir: /DataReader/src
    volumes:
      - .:/DataReader
  paginator:
    build: .
    command: python3 paginator.py
    network_mode: host
    working_dir: /DataReader/src
    volumes:
      - .:/DataReader